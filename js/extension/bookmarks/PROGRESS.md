# 书签插件重写进度

状态只使用：`未开始`、`进行中`、`待测试`、`待修复`、`已完成`、`阻塞`。

## 当前快照

- 当前提交基线：`8ce3d3b 更改css更新地址`。
- 当前代码结论：功能实现可作为技术参考，但浏览态整体设计方向错误，不能继续局部美化。
- 当前阶段：整体重写需求与设计文档；补齐搜索、tag 管理和整理模式状态机。
- 代码状态：已撤销上一轮未提交优化；本轮尚未修改业务代码。
- 用户数据事实：当前 214 条书签来自普通导入，只有目录、标题和 URL，没有标签、备注、`searchUrl` 等 extra。
- favicon 事实：应优先显示浏览器缓存中的真实图片，不能默认批量联网抓取。
- 视觉事实：Image #1 是目标整体气质，Image #2 是反例。
- 下一步：完成文档一致性检查，用户确认后再决定在现有 WXT 架构上重构浏览态，还是重写主页组件。

## 当前决策

### P-001 设计来源

状态：已完成

- Pages 页面和用户 Image #1 是浏览态唯一主基线。
- `tmp/` MarkHub 原型降级为功能状态参考。
- 现有固定侧栏、后台顶栏和蓝色主按钮结构不保留。

### P-002 数据边界

状态：已完成

- 浏览器书签是主数据。
- 普通导入没有 extra 是正常状态。
- 不自动补标签、备注、`searchUrl` 或旧 Pages JSON 数据。
- 只有用户在插件内配置后才产生 extra。

### P-003 favicon

状态：已完成

- Chrome / Edge 首选扩展原生 `/_favicon` 浏览器缓存。
- 初始加载禁止 214 个书签逐站联网抓图。
- 失败后本地降级；网络降级策略后续单独实现和验证。
- Pages 的 hover 旋转必须保留。

### P-004 浏览态重构

状态：未开始

目标：移除固定侧栏和后台式顶栏，恢复 Pages 式搜索带、内容边距、目录瀑布、居中目录头和宽松纵向书签行。

是否保留现有组件由代码审计决定，不预先承诺“全量重写”或“只改 CSS”。判断标准是：哪条路线能更可靠地满足 `DESIGN.md`，且不破坏已完成的数据写回能力。

### P-005 管理能力重排

状态：未开始

目标：把新增、整理、折叠、设置移到低干扰工具入口；整理态保留完整 CRUD、拖拽与撤销，并拆分目录 / 书签子模式，恢复进入前的搜索、tag、折叠和滚动上下文。编辑模式使用成熟组件库，不再手写弹窗、目录选择和 tag combobox。

### P-006 搜索重构

状态：未开始

目标：恢复 Pages 式搜索外观和键盘路径；完整实现 `!` 快捷搜索、`#` tag 动态候选与精确筛选、`search` / `search_site` 保留 tag。普通数据没有 tag 时正确显示空状态，不注入假目标；无自定义搜索引擎时回退 Bing；不设计旧版插件数据兼容。

### P-006A tag 筛选与编辑

状态：未开始

目标：`#xx` 动态匹配 tag 候选并预览对应书签，选择后精确筛选；左侧 tag 悬浮面板仅作为同一状态机的鼠标入口。编辑模式采用 Reka UI 或等价成熟无样式组件的 TagsInput + Combobox，支持已有 tag 补全和新 tag 创建，同时保持 Pages 视觉。

### P-007 真实数据验收

状态：未开始

目标：使用用户真实 214 条数据在 WXT 浏览器中对照 Image #1，完成桌面主页面、搜索、favicon、整理态和窄屏验收。

## 实施顺序

1. 用户确认 `REQUIREMENTS.md` 与 `DESIGN.md`。
2. 审计当前组件可复用边界，选择局部重构或主页重写。
3. 先完成纯浏览态和浏览器缓存 favicon。
4. 再接搜索浮层、折叠和低干扰工具入口。
5. 接回整理态、CRUD、拖拽、Popup 和完整备份。
6. 建立真实数据视觉回归与功能自动化。
7. 用户确认后再提交。

## 明确不做

- 不继续微调当前 Image #2 风格。
- 不把旧 Pages JSON 导入当前浏览器数据。
- 不为无 extra 数据伪造标签或快捷搜索目标。
- 不用常驻展开的固定侧栏解决标签、最近添加或设置入口；tag 只允许通过默认隐藏的左侧悬浮面板进入。
- 不以自动化通过宣称视觉通过。
- 文档确认前不修改业务代码。

## 最近记录

### 2026-07-29 16:33:52 +08:00 明确 `#` tag 搜索与成熟组件

- 用户明确这是新开发插件，不需要任何旧数据兼容；已删除无 tag `searchUrl` 的兼容与迁移设计。
- `#` 成为搜索框命令：部分输入动态匹配 tag 候选并预览并集，选择具体 tag 后进入精确筛选。
- 左侧 tag 列改为默认隐藏的悬浮面板；点击 tag 只写入 `#tag`，由同一查询解析器精确筛选，然后关闭面板并恢复搜索框焦点。
- tag 编辑改为成熟组件库的 TagsInput + Combobox，支持已有 tag 补全、新 tag 创建、去重、批量粘贴和输入法安全。
- 当前工程尚无 UI 组件库；实现阶段优先引入 Reka UI 这类无样式成熟组件，避免默认后台主题破坏 Pages 风格。
- 本轮仍只修改文档，不修改依赖或业务代码。

### 2026-07-29 16:05:49 +08:00 补齐搜索与整理需求

- 根据用户反馈补写叹号快捷搜索、普通 tag 搜索和整理模式体验。
- 从旧版 README 与实现确认保留 tag 的准确名称是 `search` 和 `search_site`，后者通过默认搜索引擎生成 `site:<domain> <关键词>`。
- 明确普通导入数据没有 tag / extra 时不产生搜索目标，整理态也不能在过滤子集上直接排序。
- 当前仍只更新文档，业务代码和 WXT 运行状态未修改。

### 2026-07-29 15:48:13 +08:00 需求重新定基线

- 用户确认上一轮和提交基线的整体样式都不符合目标，要求从头分析并先重写需求设计文档。
- 用户进一步明确：重点是整体样式风格；当前数据是普通导入书签，没有插件 extra；favicon 应来自浏览器缓存。
- 已审计 Pages HTML/CSS、Image #1 / #2、现有三份文档、历史 `REWRITE.md` 和 `ORIGIN.md`。
- 结论：旧文档同时包含 Pages 高频入口和 MarkHub 后台原型两套冲突基线，必须整体重写。
- 本轮只重写文档，不修改业务代码。
