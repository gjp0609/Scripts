# 书签插件开发状态

## 当前快照

- 基线提交：`0622e4e 保存书签插件当前开发基线`。
- 当前阶段：核心架构拆分完成，进入用户统一验收阶段。
- 浏览模式：已冻结为最高优先级不可退化边界，后续整理模式改动必须先通过浏览模式回归。
- 代码状态：`App.vue` 已从约 893 行降到约 692 行，不再拥有 Macy、Sortable、搜索 active 或工作区数组算法；仍负责页面级 CRUD、模态和导入导出用例，后续可继续拆分但不阻断本轮验收。
- 文档状态：四份旧文档已整体替换；新增 `INTERACTION.md` 与 `ARCHITECTURE.md`，把稳定规范与历史过程分离。
- 下一步：启动 WXT 持久浏览器，由用户统一验收浏览、搜索和两个整理子模式；根据真实反馈决定下一轮用例拆分或缺陷修复。

## 文档职责

| 文件 | 只负责 |
| --- | --- |
| `REQUIREMENTS.md` | 产品目标、数据契约、用户需求和完成标准 |
| `INTERACTION.md` | 状态机、键鼠流程、Esc、浮层互斥和拖拽流程 |
| `DESIGN.md` | Pages 基线、表面语义、排版和视觉阻断项 |
| `ARCHITECTURE.md` | 分层、组件边界、三种画布和迁移顺序 |
| `TEST_CHECKLIST.md` | 按模式与流程组织的回归矩阵 |
| `PROGRESS.md` | 当前阶段、风险、下一步，不保存长篇历史调试记录 |

## 架构迁移阶段

### 阶段 0：基线冻结（已完成）

- 代码已提交，可随时回退。
- 已确认用户原有的其他工作区修改不属于本插件提交。
- 当前提交不代表架构完成，只是可比较快照。

### 阶段 1：浏览模式保护（待开始）

- 为浏览模式建立真实数据回归夹具和视觉快照。
- 抽离浏览画布及持久折叠状态。
- 证明普通搜索、tag、favicon、Macy 和滚动条不退化。

当前进展：已建立 `BrowseCanvas`，浏览 Macy 固定 `trueOrder:false` 并拥有独立生命周期；查询强制展开作为明确布局输入，真实扩展回归通过。

### 阶段 2：领域与状态拆分（已完成核心部分）

- 提取纯搜索解析、候选 active 和引擎选择。
- 提取 CRUD、移动、撤销和错误恢复用例。
- 页面协调器不再直接调用浏览器 API。

当前进展：

- 已新增 `useSearchState`，统一 query、普通/`#`/`!` 解析、候选 active、引擎 selected/active 和搜索下拉互斥。
- `HeaderBar` 已改为受控组件，不再内部持有引擎 active 索引。
- 引擎循环索引已提取为纯模型并加入单元测试。
- 9 项逻辑测试、类型检查和生产构建通过；真实浏览器视觉与键鼠回归待执行。
- 工作区数组变更已提取为纯模型；reload 使用版本保护，外部书签事件串行处理。
- 当前为 14 项逻辑测试，类型检查、生产构建和真实扩展回归通过。

### 阶段 3：整理模式拆分（已完成核心部分）

- 建立整理模式-书签独立 Macy 画布和落点算法。
- 建立整理模式-目录独立 Grid 画布和预测槽。
- 两套 Sortable 生命周期互斥且可独立销毁。

当前进展：`BookmarkOrganizeCanvas` 独立持有保序 Macy，空目录和收缩目录始终保留一行无边框投放区，拖动开始不改变整体高度；命中目标后动态展开真实列表并显示唯一插入线。`FolderOrganizeCanvas` 始终使用只含标题的等高 Grid，预测顺序由应用状态渲染。`useOrganizeDrag` 统一持有 MovePlan、滚轮与边缘自动滚动，Sortable 不再决定预测 DOM 顺序。根目录完整 children 顺序已进入工作区，用于适配 Chrome 同父级绝对索引。

### 阶段 4：交互组件收口（进行中）

- 统一搜索下拉、引擎菜单、tag面板、功能菜单和模态的状态所有权。
- 使用成熟组件承载 Dialog、Select、Combobox、TagsInput 和焦点管理。
- 完成 Popup、导入导出和 favicon 刷新回归。

当前进展：Esc 只保留页面唯一窗口监听且位于冒泡阶段；组件内部菜单优先消费。遗留 Sidebar、旧搜索结果类型和未使用 Escape 工具已删除。

### 阶段 5：真实数据验收（待开始）

- 使用用户真实书签数据完成桌面和窄屏视觉确认。
- 通过 `npm run typecheck`、`npm run test`、`npm run build` 和真实扩展回归。
- 用户确认后再提交重构代码。

## 当前禁止事项

- 不在未确认架构前继续修单个截图问题。
- 不在 `App.vue` 继续增加搜索、拖拽或 Esc 分支。
- 不把临时状态文件、截图、WXT profile 或测试夹具加入提交。
- 不以自动化通过代替用户真实页面确认。
