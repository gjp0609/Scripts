# 书签浏览器插件进度文档

本文档与 `REQUIREMENTS.md` 对齐记录进度。状态只使用：未开始、进行中、待测试、待修复、已完成、暂缓、阻塞。

## 当前基线

- 当前默认工作目录：`js/extension/bookmarks`。
- 操作约束：后续实现、构建、测试文档更新、状态检查和提交记录默认都以该目录为准；除确认仓库边界外，不回到仓库根目录执行日常操作。
- 已归档旧 Vite 原型实现，归档提交：`2b3cb8c`。
- 已移除旧实现代码和过时文档。
- 当前保留资料：`REQUIREMENTS.md`、`PROGRESS.md`、`tmp/` 设计稿、`public/icon` 图标、`.gitignore`。
- 下一步从浏览器插件方式重新开始实现，不再继续修补旧 `App.vue` 原型。

## 当前执行视图

### 现在正在做什么

- 当前里程碑：`M-08 首版综合验收`
- `M-06` 已完成并通过：
  - `M-06-03` 同目录拖拽排序
  - `M-06-04` 跨目录拖拽移动
  - `M-06-06` 最近一次移动或排序撤销
  - 写回后的无明显重载体验
- 当前保留为后续体验项：
  - 目录间移动方式不够明显
  - 整理模式工具栏重复与观感问题
- 用户已确认调整方向：
  - 移除标准书签 HTML 导入导出
  - 移除仅插件额外数据导入导出
  - 只保留全量数据导入导出
  - 全量导入按“增量新增 + 同名目录合并 + 新内容尾部追加”处理
  - 全量导出文件名需要带日期时间
  - 全量导入遇到“目录名、标题、URL、标签、`searchUrl`、备注”完全相同的数据时直接跳过
- `M-07` 已完成并通过：
  - 全量导出可用，文件名带日期时间
  - 全量导入可用
  - 同名目录合并正常
  - 新内容尾部追加正常
  - 完全相同数据去重正常

### 你现在需要做什么

当前这一轮无需继续回归：

- `M-07` 已确认通过，等待进入 `M-08`

### 现在不要混进来的事项

下面这些已经记录，但不属于当前这个修复回合：

- 整理模式顶部“新增书签 / 新增目录”和页面顶部按钮重复
- 复制按钮用途不明确
- 编辑弹窗 `Esc` 关闭
- 标签支持中文逗号
- 浏览模式“更多”按钮无动作
- 不要使用浏览器原生提示
- 标签历史提示
- 内置 `search` / `site` 标签
- 默认 Bing 策略

这些会在 `M-06` 收口后，再按组进入后续轮次，不和当前排序 bug 混做。

## 需求状态

| 编号 | 需求 | 状态 | 实现文件 | 验证方式 | 遗留问题 |
| --- | --- | --- | --- | --- | --- |
| R-001 | 插件定位 | 已完成 | REQUIREMENTS.md | 文档审阅 | 无 |
| R-002 | 实现路线 | 已完成 | REQUIREMENTS.md | 文档审阅 | 具体构建工具待实现前确认 |
| R-003 | 设计来源 | 已完成 | REQUIREMENTS.md, tmp/ | 对照设计稿清单 | 无 |
| R-004 | 文档优先防丢失 | 进行中 | REQUIREMENTS.md, PROGRESS.md | 后续每轮实现前检查文档是否先更新 | 后续持续约束 |
| R-005 | 当前阶段测试文档 | 进行中 | TEST_CHECKLIST.md | 用户逐项测试并标记 | 需随里程碑推进持续更新，并及时回写已确认结果 |
| R-010 | 支持的书签范围 | 未开始 | 待定 | 真实浏览器书签树验证 | 多层目录降级口径待实现 |
| R-011 | 主数据源 | 已完成 | src/services/bookmarkApi.ts, src/services/bookmarkRepository.ts, src/app/App.vue | 浏览器书签写回验证 | 当前阶段新增、编辑、删除写回已验证通过 |
| R-012 | 插件额外数据 | 已完成 | src/services/extraStore.ts, src/services/bookmarkRepository.ts | storage 数据结构检查 | 标签、备注、`searchUrl` 保存与删除同步清理已验证通过 |
| R-013 | 浏览器外部变更同步 | 进行中 | src/app/useBookmarkWorkspace.ts, src/services/bookmarkApi.ts, src/services/bookmarkRepository.ts | 浏览器书签管理器外部修改验证 + 插件内写回后页面状态观察 | 插件内写回后的无重载体验已复测通过；仍待单独验证浏览器外部改动的同步边界 |
| R-020 | 主页面 | 已完成 | src/entrypoints/app, src/app | 扩展页面截图对比 | 当前阶段主页面问题已复测通过 |
| R-021 | Popup | 已完成 | src/entrypoints/popup, src/popup | 点击扩展按钮验证 | 当前阶段 popup 保存问题已复测通过 |
| R-022 | 编辑书签 | 已完成 | src/app/components/BookmarkModal.vue, src/app/App.vue, src/services/bookmarkRepository.ts | 编辑后浏览器书签与额外数据检查 | 用户已完成 `M-05` 当前阶段验证并通过 |
| R-023 | 添加 / 编辑目录 | 未开始 | 待定 | 目录创建、重命名、删除验证 | 删除目录风险交互需重点验证 |
| R-024 | 设置与导入导出 | 已完成 | src/services/importExportService.ts, src/app/components/ImportExportModal.vue, src/app/components/Sidebar.vue, src/app/App.vue | 全量数据导入导出与增量合并验证 | 用户已完成当前阶段验证并通过 |
| R-030 | 默认浏览体验 | 待测试 | src/app/App.vue, src/app/styles.css | 打开主页面截图与焦点验证 | 默认聚焦和基础浏览已测通，仍需继续补更多视觉验证 |
| R-031 | 书签卡片内容 | 已完成 | src/app/App.vue, src/app/components/BookmarkCard.vue, src/app/styles.css, src/app/components/SiteFavicon.vue, src/services/favicon.ts, wxt.config.ts | 截图与完整 URL 操作验证 | 用户已完成当前阶段视觉复测并通过 |
| R-032 | 目录分组 | 已完成 | src/app/App.vue, src/app/styles.css | 目录展开收起验证 | 当前阶段目录展开收起问题已复测通过 |
| R-033 | 空状态 | 未开始 | 待定 | 空数据、无结果、API 失败场景验证 | 无 |
| R-040 | 普通书签搜索 | 已完成 | src/app/App.vue, src/services/searchService.ts, src/app/components/HeaderBar.vue, src/app/components/SearchOverlay.vue | 输入、浮层、键盘选择验证 | 用户已完成 `M-04` 当前阶段验证并通过 |
| R-041 | 普通网络搜索 | 已完成 | src/app/App.vue, src/services/searchService.ts, src/app/components/SearchOverlay.vue | Google/Bing 打开验证 | 用户已完成 `M-04` 当前阶段验证并通过 |
| R-042 | 快捷站内搜索 | 已完成 | src/app/App.vue, src/services/searchService.ts, src/app/components/QuickSearchOverlay.vue | `searchUrl` 模板替换验证 | 已按最新语法修正并通过当前阶段测试 |
| R-043 | 搜索框统一输入逻辑 | 已完成 | REQUIREMENTS.md, PROGRESS.md | 文档审阅 | 已按最新口径明确普通搜索第一行是搜索引擎结果，快捷搜索采用 `!筛选词` / `!筛选词 搜索内容` 语法 |
| R-050 | 进入整理模式 | 已完成 | src/app/App.vue, src/app/components/OrganizeToolbar.vue | 模式切换截图验证 | 用户已确认可进入和退出整理模式 |
| R-051 | 整理模式工具栏 | 待修复 | src/app/components/OrganizeToolbar.vue, src/app/styles.css | 操作栏功能验证 | 功能重复与观感问题已记录，待与 M-06 一并收口 |
| R-052 | 书签整理 | 已完成 | src/app/App.vue, src/app/components/BookmarkCard.vue, src/app/useBookmarkWorkspace.ts, src/services/bookmarkApi.ts, src/services/bookmarkRepository.ts | 排序、跨目录移动、写回验证 | 当前最小整理闭环已通过；目录间移动提示等体验项留待后续 |
| R-053 | 目录整理 | 未开始 | 待定 | 目录排序、重命名、删除验证 | 当前轮先不推进目录整理 |
| R-054 | 拖拽反馈 | 进行中 | src/app/styles.css, src/app/components/BookmarkCard.vue, src/app/App.vue | 拖拽截图和撤销验证 | 当前转入撤销闭环；更清晰的目录间移动引导留待后续体验优化 |
| R-060 | 浏览器书签 API | 已完成 | src/services/bookmarkApi.ts, src/services/bookmarkRepository.ts | 真实 API 读写验证 | 当前阶段只读验证已通过；写回完整链路留在后续里程碑继续扩展 |
| R-061 | 当前标签页 API | 已完成 | src/services/extensionRuntime.ts, src/popup/Popup.vue | popup 保存当前页验证 | 当前页读取和保存入口已通过当前阶段测试 |
| R-062 | 数据模型 | 进行中 | src/types/bookmark.ts | 类型检查和数据合成单测 | 结构已拆分，但单测和真实数据边界验证未补 |
| R-063 | 服务层 | 进行中 | src/services | 模块边界审查 | 服务层已拆分，删除/移动/拖拽完整链路待继续补 |
| R-070 | 插件构建 | 待测试 | wxt.config.ts, src/entrypoints | `npm run build` + 浏览器加载扩展 | 构建已通过；manifest 已补 `favicon` 权限，等待真实浏览器加载验证 |
| R-071 | 前端技术 | 已完成 | package.json, wxt.config.ts | `npm run build` | 已使用 WXT + Vue 3 + TypeScript + lucide |
| R-072 | 禁止的实现方式 | 进行中 | 本文档 | 代码审查 | 旧实现已删除，后续持续约束 |
| R-080 | 视觉风格 | 进行中 | src/app/styles.css, src/popup/popup.css, src/app/components/SiteFavicon.vue, src/services/favicon.ts | 与 tmp 设计稿截图对比 | 主浏览态与 popup 的当前阶段视觉基准已复测通过；整理模式等后续界面仍待继续收口 |
| R-081 | 明确不要 | 进行中 | 本文档 | 代码审查和截图审查 | 后续持续约束 |
| R-082 | 响应式 | 未开始 | 待定 | 桌面/窄屏截图验证 | 无 |
| R-090 | 基础验收 | 未开始 | 待定 | 真实扩展端到端验证 | 无 |
| R-091 | 设计验收 | 未开始 | 待定 | 逐页截图对比 | 无 |
| R-092 | 交互验收 | 未开始 | 待定 | 交互清单验证 | 无 |
| R-093 | 风险验收 | 未开始 | 待定 | 删除、移动、导入等高风险场景验证 | 无 |

## 里程碑状态

| 编号 | 里程碑 | 状态 | 当前可直接测试 | 测试结果 | 是否允许进入下一里程碑 |
| --- | --- | --- | --- | --- | --- |
| M-01 | 扩展骨架可加载 | 已完成 | `npm run build`，加载 `.output/chrome-mv3/`，打开 popup 和主页面 | 用户已完成手测并通过 | 是 |
| M-02 | 真实书签只读打通 | 已完成 | 打开主页面，验证是否展示真实书签目录和卡片 | 用户已完成复测并通过 | 是 |
| M-03 | Popup 保存当前页可用 | 已完成 | 打开 popup，检查当前页信息和目录保存动作 | 用户已完成复测并通过 | 是 |
| M-04 | 书签搜索与站内搜索可用 | 已完成 | 主页面搜索框、搜索浮层、`!` 快捷搜索 | 用户已完成 `M-04` 全部当前测试项并通过 | 是 |
| M-05 | 书签编辑写回可用 | 已完成 | 添加/编辑书签弹窗、删除书签 | 用户已完成 `M-05` 全部当前测试项并通过 | 是 |
| M-06 | 整理模式最小闭环 | 已完成 | 进入整理模式、拖拽排序、跨目录移动、最近一次移动或排序撤销 | 用户已完成 `M-06-01` ~ `M-06-06` 当前测试项并通过 | 是 |
| M-07 | 导入导出最小闭环 | 已完成 | 主页面“设置与导入导出”入口、全量数据导入导出 | 用户已完成 `M-07-01` ~ `M-07-05` 当前测试项并通过 | 是 |
| M-08 | 首版综合验收 | 进行中 | 综合人工回归 | 待整理当前可测范围 | 否 |

## 最近记录

### 已结束：推进 M-05 书签写回闭环

- 当前目标：进入 `M-05`，先收口书签新增、编辑、删除的真实写回闭环。
- 本轮范围：
- 包含：书签标题、URL、所在目录、标签、备注、`searchUrl` 的保存与删除后同步清理。
- 不包含：目录新增/重命名/删除，整理模式拖拽，导入导出。
- 已确认现状：
- `BookmarkModal` 已有基础表单。
- `saveBookmarkDetails` 已具备新增/编辑与额外数据保存入口。
- `deleteBookmark` 当前只删除浏览器书签，尚未确认额外数据清理是否完整。
- 已执行：把 `M-05` 当前可测项补到 `TEST_CHECKLIST.md`。
- 已执行：新增 `deleteBookmarkDetails`，删除书签时同步清理插件额外数据。
- 已执行：主页面删除操作已切到新的删除服务链路。
- 已验证：`npm run build` 通过。
- 已确认现状：当前构建已具备 `M-05` 最小写回闭环，可以转入当前可测。
- 后续结果：用户已确认 `M-05-01` 到 `M-05-05` 全部通过，`M-05` 可收口。

### 已结束：收口 M-06-06 撤销闭环

- 当前目标：只收口 `M-06-06 最近一次移动或排序撤销`，不提前推进目录整理与其他增强项。
- 已确认现状：
  - `OrganizeToolbar` 已有撤销按钮入口。
  - `App.vue` 已有 `lastMoveSnapshot`、`undoLastMove()`、`restoreBookmarkPosition()` 接线。
  - 但测试文档里仍把撤销列为“待实现后补测”，说明当前还没有完成这一项的收口与验收口径切换。
- 本轮先做：
  - 核对撤销链路对“同目录排序 / 跨目录移动”是否都成立。
  - 若实现缺口存在，则补实现。
  - 若实现已满足，则把测试文档切到当前可测，并明确只测这一项。
- 已执行：
  - 给整理模式工具栏补充撤销按钮可用/禁用状态。
  - 补齐 `Ctrl/Cmd + Z` 全局快捷撤销，并避开输入框与编辑弹窗。
- 已验证：
  - `npm run build` 通过，当前撤销闭环已进入扩展构建产物。
- 用户复测结果：
  - `M-06-06` 通过。
  - `M-06` 当前阶段全部测试项通过。
- 当前结论：
  - `M-06` 已完成，可以进入 `M-07`。

### 已结束：进入 M-07 导入导出最小闭环

- 当前目标：开始 `M-07`，先核对现有导入导出服务与 UI 入口缺口，再确定当前最小可测闭环。
- 当前范围：
  - 全量数据导入/导出
  - 全量导入时的同名目录合并与尾部追加
  - 当前构建里的入口与错误反馈
- 当前不做：
  - 综合验收
  - 与 `M-07` 无关的整理模式体验优化
- 用户测试结果：
  - 标准书签 HTML 导入没有实际价值，且导入后只有目录没有内容。
  - 标准书签 HTML 导出没有继续保留的必要。
  - 仅插件额外数据导入导出没有实际价值，需要移除。
  - 全量导出可用，但全量导入出现 `Index out of bounds`。
- 当前修正方向：
  - 删除标准 HTML 与额外数据导入导出入口和实现。
  - 全量导入改成增量新增，不清空现有数据。
  - 同名目录直接合并，新导入内容追加到目标目录末尾。
  - “设置与导入导出”弹层样式需对齐当前主页面与现有弹窗风格。
  - 全量导出文件名改为带日期时间。
  - 全量导入补“完全相同数据”去重逻辑。
- 已执行：
  - 导入导出弹层已收缩为仅保留全量导入导出。
  - 全量导入服务已改为增量新增、同名目录合并、书签尾部追加。
  - 设置弹层结构从卡片套卡片改为与现有弹窗一致的说明 + 操作行样式。
  - 全量导出文件名已改为自动带日期时间。
  - 全量导入已补“目录名、标题、URL、标签、`searchUrl`、备注”完全相同数据的去重跳过逻辑。
- 已验证：
  - `npm run build` 通过，当前修正已进入扩展构建产物。
- 用户复测结果：
  - 全量导出文件名带日期时间。
  - 同名目录合并正常。
  - 新内容追加到目录末尾正常。
  - 完全相同数据去重正常。
- 当前结论：
  - `M-07` 已完成，可以进入 `M-08`。

### 已结束：收口写回操作的无重载体验

- 当前目标：消除修改、移动、删除等写回操作后的明显整页重载感，只修这个体验问题，不扩散到后续需求。
- 已确认现状：
  - `M-06-03` 已由用户复测通过。
  - 当前问题集中在 `saveBookmark`、`deleteBookmark`、`moveBookmarkOrder`、`undoLastMove` 等动作后显式触发 `workspace.reload()`。
  - `useBookmarkWorkspace` 里的书签监听也会再次触发整树同步，导致剩余卡片对象与 `faviconUrls` 数组整批重建，引发图标闪动。
- 本轮先修：
  - 保留浏览器书签监听用于外部同步。
  - 插件自身写回后优先做局部状态更新。
  - 监听改为按 `created / changed / removed / moved` 事件增量 patch，不再默认整树静默同步。
- 已执行：
  - `bookmarkApi` 增加结构化书签事件订阅与单节点读取入口。
  - `useBookmarkWorkspace` 改为局部 patch store：目录和书签都按受影响节点增量更新。
  - 监听层从整树 `reload` 改为按 `created / changed / removed / moved` 分支处理。
  - 保存书签、删除书签、拖拽移动、撤销移动后，主页面继续先做局部状态更新；监听回流只会命中受影响节点，不再整批重建剩余卡片对象。
- 已验证：
  - `npm run build` 通过，当前修复已进入扩展构建产物。
- 用户复测结果：
  - 修改、移动、删除等操作后，不再出现明显整页重载感。
  - 删除一项后，其余卡片图标不再闪动。
- 当前结论：
  - 这一轮问题已收口完成。

### 已结束：修复 M-06 当前阻塞项

- 当前目标：只修 `M-06` 当前测试失败项，不提前扩散到目录整理和后续增强需求。
- 当前阶段失败项：
- `M-06-03`：同目录向后拖拽排序索引错误，无法正确拖到最后一位。
- 当前阶段已通过项：
- `M-06-01`：进入和退出整理模式正常。
- `M-06-02`：整理态控件可见。
- `M-06-04`：用户已澄清跨目录拖拽功能本身没有问题。
- `M-06-05`：当前整理态样式问题已基本通过。
- 本轮先修：
- 同目录向后拖拽的目标索引换算。
- 继续保留更清晰的目录间移动提示，但不再作为当前功能阻塞。
- 已记录但暂不作为本轮阻塞：
- 整理模式顶部“新增书签 / 新增目录”与页面顶部按钮重复。
- 复制按钮用途需重新确认；若继续保留，则需要更明确文案或提示。
- 编辑弹窗需要支持 `Esc` 关闭。
- 标签分割需要支持中文逗号。
- 浏览模式右上角“更多”按钮无动作。
- 删除确认与提示信息不要使用浏览器原生样式。
- 标签历史提示、内置 `search` / `site` 标签、默认 Bing 策略属于后续需求，当前先记录。
- 后续结果：用户已确认当前排序问题修复正确，`M-06-03` 通过。

### 已结束：切换到 M-04 搜索闭环

- 用户补充规则：文档中需要明确写清当前工作目录，避免再次在仓库根目录操作。
- 已更新 `REQUIREMENTS.md`：默认工作目录固定为 `js/extension/bookmarks`，除确认仓库边界外不回根目录做日常操作。
- 已更新本文档：当前基线中写明默认工作目录与操作约束。
- 已提交：`6bcfb52` `明确书签插件工作目录约束`。
- 当前目标：按正常计划进入 `M-04`，优先实现普通搜索与快捷站内搜索的上下选择、Enter 和 Esc 闭环。
- 本轮范围：只推进 `M-04` 当前最小闭环，不提前扩散到编辑写回和整理模式。
- 已执行：给搜索框补充键盘事件分发，普通搜索与快捷站内搜索都支持上下选择。
- 已执行：普通搜索结果浮层与快捷站内搜索浮层都补充激活态高亮。
- 已执行：Enter 打开当前高亮项，Esc 清空搜索并关闭浮层。
- 已验证：`npm run build` 通过。
- 用户测试结果：`M-04-01`、`02`、`03`、`04`、`06`、`07` 通过；`M-04-05` 失败。
- 用户补充口径：普通模式下输入后直接按 `Enter`，应触发搜索引擎搜索，不是打开第一个匹配书签；搜索引擎结果必须放在第一行。
- 用户补充口径：叹号模式下，第一个空格前的内容参与站点筛选，第一个空格后的内容全部视为搜索内容；只有 `!筛选词 搜索内容` 才有搜索内容，单独 `!筛选词` 时 `Enter` 无效。
- 已更新 `REQUIREMENTS.md`：覆盖 `R-043` 搜索框统一输入逻辑，按最新口径重新定义普通搜索结果顺序和快捷搜索分词规则。
- 已执行：普通搜索结果顺序改为搜索引擎结果固定第一行，默认 `Enter` 命中搜索引擎结果。
- 已执行：快捷站内搜索分词改为“第一个空格前参与筛选，第一个空格后全部视为搜索内容”；无搜索内容时 `Enter` 无效。
- 已验证：`npm run build` 通过。
- 后续结果：用户已确认本轮修复完成，`M-04` 相关测试无问题。
- 下一步：按计划进入 `M-05`，先整理添加/编辑/删除书签写回的当前范围与可测项。

### 已结束：修正字体、图标与地址排版基准

- 用户临时需求：整体字体需要优先使用 `Sarasa Mono SC`，否则会影响后续 UI 判断。
- 用户临时需求：书签图标需要尽快换成真实 favicon，否则会影响后续 UI 判断。
- 用户临时需求：地址应在最左侧单独对齐，不和名称保持同一缩进。
- 用户临时需求：favicon 展示不要带背景。
- 用户临时需求：所有字体都要统一改掉，不能只改一部分。
- 用户临时需求：当前图标大小合适，但周围占用空间过大，需要收紧。
- 用户复测反馈：图标和文字看起来没有水平对齐。
- 用户复测反馈：调整图标占位后，卡片下方多出一点空白。
- 用户临时需求：favicon 需要按标准实现来做。
- 用户临时需求：Firefox 需要兼容，不能只按 Chrome 私有实现处理。
- 用户追问：整理模式样式是否属于当前阶段。
- 用户观察：整理模式样式仍未按前述 UI 要求收口，当前还能看到下方空白，拖拽按钮观感也不好。
- 用户追加规则：`PROGRESS.md` 每次都要保证是对的状态；已经确认修复的事项必须立即改状态，避免影响后续判断。
- 已确认：上一轮当前阶段修复已复测通过，`M-02`、`M-03` 可视为当前阶段完成。
- 已执行：先更新 `REQUIREMENTS.md`、`PROGRESS.md`、`TEST_CHECKLIST.md` 的规则和状态，避免文档继续失真。
- 已执行：主页面和 popup 字体栈统一优先切到 `Sarasa Mono SC`。
- 已执行：新增通用 favicon 逻辑，书签卡片与搜索浮层优先显示真实 favicon，失败时再回退字母。
- 已执行：favicon 展示去掉背景；书签地址改成卡片内最左侧独立一行，不再跟标题同缩进。
- 已执行：把字体收成统一变量，主页面、popup 和地址文本全部走同一套字体栈。
- 已执行：压缩 favicon 容器占位，保留图标大小，收紧卡片和浮层中的图标周围留白。
- 已执行：按 16px 图标高度重排标题行，补齐标题行行高，并去掉普通卡片遗留的最小高度。
- 已执行：favicon 来源改为标准实现，Chrome 优先走扩展原生 `_favicon`，Firefox 兼容路径优先走站点 `favicon.ico`，第三方服务只做后备。
- 已执行：manifest 补充 `favicon` 权限，支持 Chromium 原生 favicon 读取。
- 已验证：`npm run build` 通过，WXT 构建产物已更新。
- 已确认范围：整理模式对应 `M-06 / R-050 / R-051 / R-052 / R-053 / R-054`，当前不属于正在验收的 `M-01` 到 `M-03` 与 UI 基准补充复测范围。
- 已记录后续问题：整理模式后续需要重点收口卡片底部空白、拖拽按钮样式和整体对齐。
- 后续结果：用户已确认本轮相关修复全部验证通过；整理模式样式问题保留到 `M-06` 再单独处理。

### 已结束：根据复测继续收口 M-02 与 M-03

- 用户复测反馈：
- 地址虽然显示全了，但超出后折行，不要换行。
- 折叠状态箭头现在朝上，收起状态应朝左。
- 无标签网站卡片下方仍有空白。
- popup 成功保存时没必要展示提示，应直接关闭；只有失败时才提示且不关闭。
- 已执行：继续只修这 4 个当前阶段问题，并再次交给用户复测。

### 已结束：修复 M-02 与 M-03 当前阻塞项

- 当前轮目标：只修当前阶段失败项，不提前推进后续里程碑功能。
- 已修复：
- `M-02-03` 书签卡片 URL 显示不全，不应隐藏前缀。
- `M-02-04` 目录收起状态箭头方向反了。
- `M-03-06` popup 保存反馈位置不对，用户看不到。
- 已验证：`npm run build` 通过。
- 后续结果：用户已按 `TEST_CHECKLIST.md` 完成复测并通过。

### 已结束：录入当前阶段测试结果

- 用户已完成 `TEST_CHECKLIST.md` 当前可测部分的手工测试。
- 当前阶段通过项：`M-01` 全部通过；`M-02` 的真实书签读取、标题显示、默认聚焦、搜索框浮层对齐通过；`M-03` 的当前页读取、目录展示、目录搜索、保存动作通过。
- 当前阶段待修复项：
- `M-02-03`：书签卡片 URL 显示不全，不应隐藏前缀。
- `M-02-04`：目录展开收起可用，但收起状态箭头方向反了。
- `M-03-06`：保存反馈出现在 popup 底部，不在可见区域。
- 已处理边界：测试文件中的 favicon 来源和字体要求先保留为“超出当前阶段的观察项”，当前不阻塞 `M-01`、`M-02`、`M-03`。

### 已结束：整理记录状态与测试边界

- 用户临时需求：最近记录里不应同时存在多个“当前轮”，需要有结束状态。
- 用户临时需求：只有在实现之后，才能知道哪些内容需要进入当前测试范围。
- 已更新 `REQUIREMENTS.md`：补充最近记录只能有一个进行中的“当前轮”，旧条目必须改成“已结束”状态。
- 已更新 `REQUIREMENTS.md`：补充测试文档只能把已实现功能放进“当前可测”，未实现内容必须放进“待实现后补测”。
- 准备执行：整理最近记录标题，并重写测试清单结构，区分“当前可测”和“待实现后补测”。

### 已结束：细化状态定义

- 用户临时需求：状态不够精准，至少需要 `未开始 / 进行中 / 待测试 / 待修复 / 完成`。
- 已更新 `REQUIREMENTS.md`：状态集合改为 `未开始 / 进行中 / 待测试 / 待修复 / 已完成 / 暂缓 / 阻塞`，并补充每个状态的定义。
- 已更新本文档：需求状态和里程碑状态按新状态重新落位。
- 当前口径：凡是“代码已接入当前构建产物，但仍需你在真实浏览器验证”的，一律使用 `待测试`，不再混用 `进行中` 和 `已完成`。

### 已结束：新增当前阶段测试文档

- 用户临时需求：需要单独测试文档，列出当前阶段需要测试的内容，供逐项标记。
- 已更新 `REQUIREMENTS.md`：新增 `R-005 当前阶段测试文档`，明确测试文档必须独立维护、只覆盖当前阶段范围。
- 已执行：创建 [TEST_CHECKLIST.md](/abs/path/R:/Files/Workspace/Mine/Scripts/js/extension/bookmarks/TEST_CHECKLIST.md)。
- 当前测试范围：只覆盖 `M-01`、`M-02`、`M-03`，并明确 `M-04` 及之后暂不测试。

### 已结束：修正搜索浮层宽度与定位

- 用户临时需求：`search-overlay` 和 `quick-overlay` 出现偏移，且宽度与搜索框不一致。
- 已确认原因：旧实现使用静态 `calc(...)` 定位和固定 `440px` 宽度，没有绑定真实搜索框 DOM 尺寸。
- 已执行：改为运行时读取搜索框的 `getBoundingClientRect()`，让两个浮层跟随真实搜索框的左边界、顶部和实际宽度。
- 已验证：`npm run build` 通过，WXT 构建成功。
- 遗留问题：仍需用户在真实浏览器里确认视觉对齐是否与设计稿一致。

### 已结束：修正搜索浮层定位

- 用户临时需求：`search-overlay` 和 `quick-overlay` 与搜索框没有对齐。
- 已确认原因：当前实现使用固定 `calc(...)` 定位，未绑定真实搜索框 DOM 坐标；左右工具区宽度变化后会产生偏移。
- 已确认补充问题：浮层宽度仍沿用旧的 `440px`，与真实搜索框宽度不一致。
- 准备执行：将两个浮层改为跟随真实搜索框的运行时定位，并直接使用搜索框实际宽度。

### 已结束：补充里程碑测试节点

- 用户临时需求：需求文档中补充里程碑节点，供中途直接测试，避免全部做完才发现问题。
- 已更新 `REQUIREMENTS.md`：新增 `M-01` 到 `M-08` 里程碑，逐个定义目标、通过标准和测试方式。
- 已更新本文档：新增“里程碑状态”表，明确每个里程碑当前是否可测、测试结果和是否允许进入下一阶段。
- 当前口径：`M-01` 视为已完成，`M-02` 与 `M-03` 为进行中，其余里程碑尚未进入可交付测试阶段。

### 已结束：切换到 WXT

- 用户临时需求：改为使用 WXT。
- 技术决策：后续使用 WXT 作为浏览器插件构建框架，不继续维护手写 Vite 多入口扩展壳。
- 已执行：迁移工程到 WXT 结构，新增 `wxt.config.ts`，将 popup、主页面和 background 移入 `src/entrypoints/`，删除旧 `app.html`、`popup.html`、`vite.config.ts`、`public/manifest.json` 和旧 `src/background.ts`。
- 已执行：保留已拆出的服务层和 Vue 组件，按 WXT 入口重新挂载。
- 已执行：浏览器 API 从 `chrome` 回调风格切换为 WXT `browser` Promise API。
- 已验证：`npm run build` 通过，生成 `.output/chrome-mv3/manifest.json`、`app.html`、`popup.html`、`background.js`、图标和资源文件。
- 验证结果：生成的 manifest 为 MV3，包含 `bookmarks`、`storage`、`activeTab`、`tabs`、`scripting` 权限，`action.default_popup` 为 `popup.html`。
- 遗留问题：尚未在真实浏览器中加载 `.output/chrome-mv3/` 做书签 API 读写和截图对比。

### 已结束：记录规则修正

- 用户临时需求：操作记录应当写入“最近记录”，并按时间倒序排列，最新记录在最上面。
- 用户临时需求：不要用日期式排期；当前阶段大约是一到两天工作量。
- 已更新 `REQUIREMENTS.md`：补充最近记录倒序规则、短周期工作量口径和避免远期日期式排期的约束。
- 已更新本文档：移除独立的“当前轮动作”区，将本轮动作并入“最近记录”。

### 已结束：浏览器插件工程基线

- 已提交文档优先执行纪律，提交：`b8c0265`。
- 本轮先创建新的浏览器插件工程，不恢复旧 `src/App.vue` 原型。
- 原计划技术路线：使用 Vue 3 + TypeScript + Vite 自建扩展壳；该路线已被上方“切换到 WXT”记录替代。
- 设计执行：以 `tmp/` 的 MarkHub 设计稿为样式基准，抽取统一 CSS tokens，优先实现 240px 侧栏、56px 顶栏、紧凑书签卡片、搜索浮层、整理模式工具栏、添加/编辑弹窗和 popup 基础布局。
- 数据执行：先搭建 `bookmarkApi`、`extraStore`、`bookmarkRepository`、`searchService`、`extensionRuntime`、`importExportService`，保持浏览器书签数据和插件额外数据分离。
- 导入导出执行：本轮要预留并实现基础服务函数，覆盖标准 Netscape Bookmark HTML 导入/导出、全量数据导入/导出、仅插件额外数据导入/导出。
- 验证执行：安装依赖后至少执行一次构建，确认扩展产物能生成；真实浏览器加载和截图对比后续继续补充。

### 基线归档与需求整理

- 新增 `REQUIREMENTS.md`，将目标改为直接实现浏览器插件。
- 归档旧实现提交：`2b3cb8c`。
- 移除旧 Vite 原型实现、旧 README、旧 REWRITE、旧构建配置和依赖目录。
- 新增本文档作为后续进度基线。
- 将 `tmp/` 设计稿纳入本次重开基线。
- 新增执行纪律：后续实现前，必须先把关键上下文、用户临时需求和准备执行的动作更新到文档，再开始代码或构建改动，防止上下文压缩后信息遗失。
