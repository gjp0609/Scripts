# 书签插件重写进度

状态只使用：`未开始`、`进行中`、`待测试`、`待修复`、`已完成`、`阻塞`。

## 当前快照

- 当前文档基线：`a905dbd 重写书签插件需求与交互设计`。
- 当前代码结论：整理态筛选拖拽、左侧工具轨道、全局直接输入、普通搜索方向键、`!` 候选边界和搜索下拉动态对齐均已实现并通过隔离 Chrome。
- 当前阶段：本轮组合优化已完成自动回归，准备重启 WXT 供用户真实书签确认。
- 代码状态：业务代码尚未提交；最新类型检查、8 项逻辑测试、生产构建及三组真实 Chrome 回归通过，用户真实数据视觉验收仍未完成。
- 用户数据事实：当前 214 条书签来自普通导入，只有目录、标题和 URL，没有标签、备注、`searchUrl` 等 extra。
- favicon 事实：应优先显示浏览器缓存中的真实图片，不能默认批量联网抓取。
- 视觉事实：Image #1 是目标整体气质，Image #2 是反例。
- 下一步：用户在已重启的最新 WXT dev 持久 profile 中确认页面；按真实反馈收口后再提交业务代码。
- 术语约束：统一使用搜索框、搜索下拉、tag按钮、功能按钮、书签部分、书签搜索结果、整理模式七个名称。

### 2026-07-31 整理工具条视觉收敛

- 用户截图确认整理模式工具条仍存在固定宽度过宽、弹性空档、分段嵌套边框和贴近目录边框的问题。
- 工具条改为按内容收缩的连续操作带；移除弹性占位和分段外框，保留工具条唯一固定边界，目录 / 书签通过 selected 背景表达状态，并恢复与书签部分的垂直间距。
- 桌面继续显示“书签 / 目录”文字，仅极窄屏退化为图标。类型检查、8 项逻辑测试、生产构建和隔离 Chrome 整理态回归通过；桌面工具条宽 314px，390px 窄屏与 tag按钮保留 12px 间距且无溢出。

## 当前决策

### 2026-07-31 目录拖拽对齐与落点预测

- 用户要求目录移动时统一折叠以完全对齐，并在拖动过程中明确显示预计落点。
- 实现采用目录整理态的临时视觉强制收缩，不写入用户折叠偏好；等高目录改用正常流 CSS Grid，拖动副本脱离 Grid，Grid 内由随预测索引移动的等高虚线槽表达最终位置。
- 用户实测指出预测槽与实际落点不一致、目录不需要空内容、浏览态全部收缩会导致书签整理无可拖动内容。根因是可见目录相对下标被用于 Chrome 绝对索引，以及书签整理沿用了持久折叠状态；已改为绝对目录锚点写回、目录仅标题行、书签整理临时全展开。
- 用户继续纠正不能以移除展开 / 收缩解决折叠死状态，并指出书签拖拽时鼠标滚轮失效、目录放下有瞬间回跳。已恢复书签整理临时展开 / 收缩，书签拖拽切换为绑定 ScrollArea 的 fallback 模式，目录写回前先按预测位置乐观排序以避免 Vue 恢复旧 DOM。
- 滚轮真实回归首轮进一步定位到临时收缩会卸载书签列表，而再次展开后 Sortable 仍绑定旧 DOM；已将整理态临时折叠集合加入 Sortable 重建依赖，确保每次展开都绑定当前投放容器。
- 最终专项回归通过：8 个目录预设为浏览态收缩后，书签整理仍默认展开；收缩再展开后 fallback 副本位于 `BODY`，按住拖拽时滚轮将 ScrollArea 从 0 滚到 520。目录整理 7 个可见目录均为 38px 标题行，预测槽位于索引 6；松手后 16ms 的 DOM 顺序、含直属书签的 Chrome 真实目录顺序均与预测一致。
- 用户截图确认整理模式-书签整体收缩后仍渲染 `...` 空内容，且临时折叠未触发 Macy 重排，导致第二行保留展开态旧坐标并出现巨大空档。已明确区分书签 Macy 与目录 Grid：本次仅修书签收缩为标题行并将临时折叠集合加入 Macy 重排依赖。
- 用户进一步明确整理模式-书签的收缩目录仍是跨目录移动目标，不能像目录模式一样移除内容容器。已修正为：书签模式保留一行高真实 Sortable 投放区但不显示 `...`；目录模式继续只保留标题行。
- 用户指出常驻一行投放区视觉丑陋，应按拖拽目标动态适应高度。方案调整为零高度 Sortable 容器：平时标题行紧凑排列，拖动靠近时仅目标目录展开投放区并重排 Macy，离开收回，放下后目标自动展开。
- 代码与文档已开始修改，待真实 Chrome 拖拽中途截图和最终顺序回归。

### P-001 设计来源

状态：已完成

- Pages 页面和用户 Image #1 是浏览态唯一主基线。
- `tmp/` MarkHub 原型降级为功能状态参考。
- 现有固定侧栏、后台顶栏和蓝色主按钮结构不保留。

### P-002 数据边界

状态：已完成

- 浏览器书签是主数据。
- 普通导入没有 extra 是正常状态。
- 不自动补标签、备注、`searchUrl` 或旧 Pages JSON 数据。
- 只有用户在插件内配置后才产生 extra。

### P-003 favicon

状态：已完成

- Chrome / Edge 首选扩展原生 `/_favicon` 浏览器缓存。
- 初始加载禁止 214 个书签逐站联网抓图。
- 失败后本地降级；网络降级策略后续单独实现和验证。
- Pages 的 hover 旋转必须保留。

### P-004 浏览态重构

状态：待测试

目标：移除固定侧栏和后台式顶栏，恢复 Pages 式搜索带、内容边距、目录瀑布、居中目录头和宽松纵向书签行。

是否保留现有组件由代码审计决定，不预先承诺“全量重写”或“只改 CSS”。判断标准是：哪条路线能更可靠地满足 `DESIGN.md`，且不破坏已完成的数据写回能力。

### P-005 管理能力重排

状态：待测试

目标：把新增、整理、全部展开 / 收缩、favicon 刷新和设置收纳到默认折叠、悬浮展开的低干扰工具入口；整理态保留完整 CRUD、搜索、tag、拖拽与撤销，并拆分目录 / 书签子模式。编辑模式使用成熟组件库，不再手写弹窗、目录选择和 tag combobox。

### P-006 搜索重构

状态：待测试

目标：恢复 Pages 式搜索外观和键盘路径；完整实现 `!` 快捷搜索、`#` tag 动态候选与精确筛选、`search` / `search_site` 保留 tag。普通数据没有 tag 时正确显示空状态，不注入假目标；无自定义搜索引擎时回退 Bing；不设计旧版插件数据兼容。

### P-006A tag 筛选与编辑

状态：待测试

目标：`#xx` 动态匹配 tag 候选并预览对应书签，选择后精确筛选；左侧 tag 悬浮面板仅作为同一状态机的鼠标入口。编辑模式采用 Reka UI 或等价成熟无样式组件的 TagsInput + Combobox，支持已有 tag 补全和新 tag 创建，同时保持 Pages 视觉。

### P-007 真实数据验收

状态：进行中

目标：使用用户真实 214 条数据在 WXT 浏览器中对照 Image #1，完成桌面主页面、搜索、favicon、整理态和窄屏验收。

### P-008 统一表面语义与最短操作路径

状态：进行中

目标：固定内容只用单层边框；tag、工具、搜索、引擎和表单菜单只在激活时形成统一无描边阴影表面；点击或 Enter 立即执行并关闭；编辑、确认和导入导出收敛为无外描边单层模态。取消多层边框、标题底、内层卡片、重复阴影和教学文字。

实施范围：`styles.css`、`App.vue`、`HeaderBar.vue`、`TagPanel.vue`、两类搜索浮层、书签编辑菜单、四类 Dialog 与整理工具条。明确不修改 favicon 缓存、搜索解析、Macy 48/24px 网格和浏览器书签数据层。

## 实施顺序

1. 用户确认 `REQUIREMENTS.md` 与 `DESIGN.md`。
2. 审计当前组件可复用边界，选择局部重构或主页重写。
3. 先完成纯浏览态和浏览器缓存 favicon。
4. 再接搜索浮层、折叠和低干扰工具入口。
5. 接回整理态、CRUD、拖拽、Popup 和完整备份。
6. 建立真实数据视觉回归与功能自动化。
7. 用户确认后再提交。

## 明确不做

- 不继续微调当前 Image #2 风格。
- 不把旧 Pages JSON 导入当前浏览器数据。
- 不为无 extra 数据伪造标签或快捷搜索目标。
- 不用常驻展开的固定侧栏解决标签、最近添加或设置入口；tag 只允许通过默认隐藏的左侧悬浮面板进入。
- 不以自动化通过宣称视觉通过。

## 最近记录

### 2026-07-31 09:12:22 +08:00 筛选状态恢复真实相对位置拖拽

- 移除“查询非空即关闭 Sortable”的保护，筛选期间继续显示拖拽把手并允许目录、同目录书签和跨目录书签移动。
- 新增可见锚点映射：拖动后的下一可见项优先作为插入前锚点，没有下一项时插入前一可见项之后；最终位置换算到去除拖动项后的完整数组，再按 Chrome 同父级移动语义转换 API 索引。
- 书签整理筛选时保留全部目录容器作为投放目标，只过滤内部书签；目录整理继续过滤目录本身。
- 隔离 Chrome 实测三类真实拖拽均通过：完整顺序分别为隐藏项 / 匹配项 / 拖动项 / 隐藏项，跨目录隐藏项也未被挪动；页面无控制台错误。
- 用户进一步要求消除左右对角入口；tag 与工具已合并为左侧纵向轨道，工具位于 tag 下方并向下展开，无 tag 时自动上移。
- 搜索新增全页面直接输入：无修饰键普通字符会聚焦搜索框并保留首字符，输入法启动键只转移焦点；其他表单和模态不受影响。`!` 候选仅保留 `search_site`，清除 `search` 普通引擎结果，同时保留左侧当前引擎；普通搜索上下键继续循环切换引擎。
- 搜索下拉定位改为在查询变化、引擎切换和重新显示后重测搜索框真实 DOM 边界，消除不同宽度引擎名称导致的横向错位。

### 2026-07-30 19:50:49 +08:00 整理模式搜索、布局与 CRUD 修复

- 整理态不再绕过过滤器：普通文本和 `#tag` 均直接筛选当前画布。此处最初曾暂停筛选状态的 Sortable，已在 2026-07-31 改为真实可见锚点映射，当前不再禁用拖拽。
- 移除无意义的“完成”和复制 URL；浏览态及整理态统一使用展开 / 收缩图标，整理工具条新增整体展开与收缩。
- 删除整理态 CSS Grid 和强制静态定位，整理态使用 Macy `trueOrder: true` 保序布局且保持内容自然高度；浏览态继续使用 `false` 最短列布局。
- Esc 改为每次只回退一层；编辑 / 确认弹窗关闭后仍保留整理态，候选、查询和整理态依次退出。
- 确认弹窗不再依赖自动关闭的 Action 事件顺序；书签和目录删除增加错误保护。编辑切换目录时显式使用目标目录 children 数量作为移动索引，确保追加到尾部。
- `vue-tsc`、8 项逻辑测试和 Chrome MV3 生产构建通过。隔离 Chrome 实测删除真实节点、跨目录尾部移动、搜索 / tag、整体展开 / 收缩和 Macy 布局均通过，无控制台错误。

### 2026-07-30 17:34:24 +08:00 普通文本搜索取消浮层

- 用户明确：不带 `!` / `#` 的普通输入不需要搜索弹窗，只用于页面书签筛选或按 Enter 网络搜索。
- 从 App 状态机中移除普通搜索结果数组、书签 active 索引、方向键候选和结果打开分支；普通文本只驱动 `filterFolders`，Enter 始终调用当前引擎。
- `SearchOverlay` 收窄为 `#` tag 候选专用，`QuickSearchOverlay` 继续只服务 `!`；浮层只在两个命令前缀存在时渲染，并继续与引擎菜单互斥。
- 四份正式文档已同步；类型检查、8 项逻辑测试和生产构建通过。隔离 Chrome 实测普通文本浮层数为 0、页面匹配书签为 18，输入 `#` 后候选层及既有交互回归正常。
- 持久 WXT 已再次完整重启，最新 Node PID `11572`、Chrome 主 PID `2752`，用户当前浏览器加载的是本次规则。

### 2026-07-30 15:37:14 +08:00 逐项完成真实浏览器交互验证

- 将引擎列表拆为 selected 当前引擎和 active 鼠标 / 键盘目标；搜索结果 active 使用同一强背景，hover 保留浅背景。隔离浏览器中同时停留 hover 第 2 项、键盘 active 第 3 项时，两者背景可直接区分。
- 实测引擎打开时搜索浮层为 0，总可见浮层为 1；搜索长列表 `offsetWidth` 与 `clientWidth` 同为 480px，滚动槽不占内容宽度。
- 实测发现右侧弱底板被更高优先级的 `.utility-dock button` 覆盖，修正后左右计算背景均为 `rgb(245, 246, 245)`，默认阴影均为 none。
- 扩展 Esc 测试时发现指针停在工具入口上收起后无法原地重开；为右侧主入口补充点击打开。随后搜索、引擎、tag、工具组、整理态、四类 Dialog 的 Esc 恢复全部通过。
- 已审查默认态、搜索展开态和引擎展开态截图，无控制台错误；下一步重启 WXT dev，确保用户看到的是最新代码。
- 旧 WXT Node / Chrome 已精确停止并以同一持久 profile 重启；最新 Node PID `13592`、Chrome 主 PID `22032`，dev 构建成功并已打开浏览器。

### 2026-07-30 15:01:01 +08:00 统一 active / hover、互斥浮层与 Esc 恢复

- 用户实测指出：引擎与搜索结果的 active / hover 语义仍不一致，搜索 hover 未影响键盘锚点，列表滚动槽占宽，且引擎和搜索浮层会同时出现。
- 搜索结果 hover 改为同步对应 active 索引，active 使用 selected 背景、纯 hover 使用浅背景；长列表隐藏占宽的原生滚动槽但保留滚轮与触控滚动；引擎菜单打开时主搜索浮层停止渲染。
- 左右默认入口从透明悬空图形改为同值弱中性底板，继续保持无边框、无默认阴影。
- 全局 Esc 改为窗口捕获级恢复：关闭局部浮层和可关闭模态、清空查询、退出整理并聚焦搜索框；导入写入中的不可中断保护保留。
- 用户进一步确认顶部搜索带不能只用细线；恢复克制的向下阴影，作为下方内容独立滚动时的层级例外。

### 2026-07-30 14:53:04 +08:00 修复引擎悬浮切换模型

- 用户实测首先发现引擎切换存在操作 bug。根因是标准 Select 自带点击开关和 pointer 生命周期，与外部强制的悬浮打开 / 延迟关闭状态互相竞争。
- 将主页引擎入口改为受控 Popover：悬浮或聚焦只打开，入口到菜单保留 140ms 横移缓冲，点击具体引擎立即切换并关闭；内置来源说明删除，当前项仍用统一 selected 背景和勾选。
- 表单内目录选择继续使用成熟 Select，不把主页命令菜单规则误套到输入字段。
- 最新类型检查、8/8 逻辑测试和 Chrome MV3 生产构建通过，待用户在当前 WXT 页面验证实际指针操作。

### 2026-07-30 14:35:28 +08:00 统一最短操作路径与表面语义

- 用户确认界面核心不是把六种弹窗做成同类卡片，而是让视觉状态对应“静止、激活、确认”三个操作阶段。
- 正式确定：固定结构使用边框；默认交互入口无外观；悬浮激活使用统一阴影；当前目标使用背景；点击或 Enter 立即执行并结束；只有必要表单、危险确认和文件操作进入单层模态。
- 本轮先同步需求、设计、测试和进度文档，再实施搜索带、左右入口、搜索 / 引擎 / 编辑菜单、Dialog 和整理工具条；favicon、搜索逻辑、Macy 高度网格和数据层不回退。

### 2026-07-30 09:41:18 +08:00 精确恢复 Pages 行高与滚动细节

- 根据用户纠正，目录头恢复 Pages 的 `0.4rem` 上下内边距与自然高度，不再拉高到 `3rem`；普通书签与折叠 `...` 行仍为 `3rem`，Macy 目录纵向间距由 24px 收回到 Pages 的 16px。
- 用户截图进一步定位到展开列表有 `0.8rem` 标题下间距、折叠行没有该间距，导致两列后续目录错开约 13px；已给折叠行补齐相同间距，使目录高度差回到完整 `3rem` 书签行倍数。
- 经完整高度计算后改为确定值：目录头 36px、内容前距 10px、目录外边框共 2px，固定结构正好 48px；书签与折叠行均为 48px，空列表不再引入额外最小高度；目录间距改为 24px，使边框相位由 `0 / 16 / 32px` 压缩为 `0 / 24px`。
- 书签名称 hover 首次改用 `scale(1.05)` 后用户发现中间帧模糊；现改为固定 `1.4rem` 行盒内从 `1rem` 到 `1.05rem` 的字号过渡，避免纹理缩放，同时由固定行盒约束垂直位置。
- 放宽标题实际行高，hover 字号变化不再裁切字形上下缘，也不改变书签固定高度。
- 主内容改用 Reka UI 覆盖式 ScrollArea，原生滚动槽隐藏，细滑块覆盖在视口右缘且不挤占 5% 内容边距。
- favicon 改为本地 Globe 占位常驻底层、浏览器 `/_favicon` 缓存图片原位覆盖；移除 lazy 延迟，真实图与 fallback 均随书签 hover 旋转。
- 核对生成 manifest 已包含 `favicon` 权限；WXT profile 改为绝对路径持久化，避免相对路径被 Chrome 从安装目录错误解析。示例站点已在当前 profile 缓存 16×16 和 32×32 图片；SPA 候选调整为精确书签地址优先，再回退去 fragment 地址和站点根地址，避免未命中的默认图遮住精确缓存。
- 新增工具区“刷新全部图标”入口：以全局刷新版本号重置所有 favicon 候选，并仅对扩展原生 `/_favicon` 请求执行缓存穿透；搜索引擎、书签、搜索结果和之后展开的目录共享同一刷新状态，不逐站联网。
- 根据实测补充页面启动初始化 favicon 读取版本，修复手动全量刷新已出现真图、刷新插件页面后又命中旧默认图的退化。
- 右下工具组改为默认单个 `Menu` 图标，悬浮或聚焦时向上展开；接入已有批量折叠接口，新增全部展开与全部折叠图标命令并持久化状态。
- 根据实测将工具图形由 17px 收到 15px，保留 36px 命中区，并移除工具组最外层边框。
- 继续按左侧 tag 按钮校准：移除工具组容器 5px 内边距、白底和阴影，默认整体由约 46px 收到实际 36px；左右主入口统一为 15px 图形、无边框、同值轻阴影，右下展开操作项不再各自叠加阴影。
- 左侧 tag 改为悬浮触发，入口与 Portal 面板之间加入 140ms 关闭缓冲；点击和键盘聚焦继续兼容。全局隐藏浏览器原生 focus outline，但保留组件自有焦点与选中状态。
- 类型检查与 8 项逻辑单测通过，待用户当前 WXT 浏览器真实数据验收。

### 2026-07-30 09:17:48 +08:00 收紧自用界面与弹窗交互

- 明确“自用、不教学”：搜索框不显示 placeholder，引擎入口不显示下拉箭头或重复提示，只保留 favicon、名称和不可见无障碍信息。
- 自定义 Bing / Google 按名称及服务域名覆盖对应内置项；菜单当前项与高亮项使用中性背景，不使用组件库焦点外框。
- 空搜索框恢复 `↑` / `↓` 循环切换当前可见引擎。
- 添加书签的目录 Select 和 tag Combobox Portal 提升到 Dialog 上层；目录菜单实测层级 230，高于弹窗 210。
- tag 编辑改为 token 与输入共享同一外框、焦点圈和换行流，建议菜单以整个字段为锚点。
- 最新类型检查和 8 项逻辑单测通过；以上视觉项目继续由用户在当前 WXT dev 中直接验收。

### 2026-07-29 17:58:46 +08:00 按真实 Pages 参数校准浏览态

- 根据用户真实数据反馈关闭 Macy `trueOrder`，浏览态改为最短列优先重排，避免个别列明显长尾；浏览器顺序仍作为数据与整理写回顺序。
- 页面改为搜索带固定、主内容独立滚动；滚动槽透明，细滑块在悬浮时出现。
- 完全恢复 Pages 字体栈顺序；目录标题改为 `1.1rem` 常规字重并删除折叠箭头。
- 书签结构恢复为 favicon + `1rem` 标题同一行、`0.62rem` URL 从整行左缘开始，固定 `3rem` 高。
- 搜索区拆回独立引擎入口和 30rem 搜索框，移除组合大胶囊与多余搜索图标；内置引擎图标继续使用浏览器 `/_favicon` 缓存。
- 类型检查、5 项搜索单测、生产构建和隔离 Chrome MV3 回归通过；1858px 4 列、0 重叠，390px 无横向溢出。真实 214 条数据下的滚动条和列平衡仍待用户复测。

### 2026-07-29 17:29:38 +08:00 完成第一轮实现与隔离 MV3 回归

- 使用 `Reka UI 2.10.1` 迁移书签、目录、确认和导入导出弹窗，目录选择与 tag 建议使用成熟 Select / Combobox / TagsInput 组件。
- 统一实现普通搜索、`!` 快捷搜索和 `#` tag 搜索；左侧 tag 面板点击只写入 `#tag`，真实浏览器验证焦点回到搜索框且搜索浮层保持关闭。
- 浏览态恢复 Pages 式居中搜索、白底、目录 Macy 瀑布和目录内纵向书签；1858px 隔离数据下为 4 列、0 重叠，390px 无横向溢出。
- favicon 仅使用 override 与浏览器 `/_favicon` 缓存来源；书签 hover 旋转保留。
- 5 项搜索服务单测、`vue-tsc`、WXT 生产构建通过；隔离 Chrome MV3 无控制台错误，三类弹窗和 tag `Tab` 补全通过。
- 尚未完成用户真实 214 条数据、真实 favicon 命中、拖拽写回/撤销和导入导出的最终人工验收，因此业务代码保持未提交。

### 2026-07-29 16:33:52 +08:00 明确 `#` tag 搜索与成熟组件

- 用户明确这是新开发插件，不需要任何旧数据兼容；已删除无 tag `searchUrl` 的兼容与迁移设计。
- `#` 成为搜索框命令：部分输入动态匹配 tag 候选并预览并集，选择具体 tag 后进入精确筛选。
- 左侧 tag 列改为默认隐藏的悬浮面板；点击 tag 只写入 `#tag`，由同一查询解析器精确筛选，然后关闭面板并恢复搜索框焦点。
- tag 编辑改为成熟组件库的 TagsInput + Combobox，支持已有 tag 补全、新 tag 创建、去重、批量粘贴和输入法安全。
- 当前工程尚无 UI 组件库；实现阶段优先引入 Reka UI 这类无样式成熟组件，避免默认后台主题破坏 Pages 风格。
- 本轮仍只修改文档，不修改依赖或业务代码。

### 2026-07-29 16:05:49 +08:00 补齐搜索与整理需求

- 根据用户反馈补写叹号快捷搜索、普通 tag 搜索和整理模式体验。
- 从旧版 README 与实现确认保留 tag 的准确名称是 `search` 和 `search_site`，后者通过默认搜索引擎生成 `site:<domain> <关键词>`。
- 明确普通导入数据没有 tag / extra 时不产生搜索目标，整理态也不能在过滤子集上直接排序。
- 当前仍只更新文档，业务代码和 WXT 运行状态未修改。

### 2026-07-29 15:48:13 +08:00 需求重新定基线

- 用户确认上一轮和提交基线的整体样式都不符合目标，要求从头分析并先重写需求设计文档。
- 用户进一步明确：重点是整体样式风格；当前数据是普通导入书签，没有插件 extra；favicon 应来自浏览器缓存。
- 已审计 Pages HTML/CSS、Image #1 / #2、现有三份文档、历史 `REWRITE.md` 和 `ORIGIN.md`。
- 结论：旧文档同时包含 Pages 高频入口和 MarkHub 后台原型两套冲突基线，必须整体重写。
- 本轮只重写文档，不修改业务代码。
