# 书签插件验收清单

当前阶段是第一轮实现后的真实数据验收。`[x]` 仅表示已由文档确认、单元测试或隔离 Chrome MV3 回归验证；真实 214 条数据、favicon 命中和整理写回仍需用户测试。

## A. 文档基线确认

- [x] A-01 浏览态的整体风格以 Pages 和 Image #1 为准，而不是 `tmp/` MarkHub 原型
- [x] A-02 浏览态不使用固定侧栏、Logo、产品说明和后台式管理顶栏
- [x] A-03 浏览态采用居中搜索带、白底、5% 内容边距和结构性留白
- [x] A-04 主内容采用目录多列瀑布流，目录内书签纵向排列
- [x] A-05 目录标题居中且保持紧凑的 36px；固定结构、书签与折叠 `...` 行均按 48px 计算，目录块纵向间距为 24px
- [ ] A-06 管理能力保留，但只能通过低干扰入口进入第二层
- [ ] A-07 普通导入书签没有 extra 是正常状态，界面不伪造标签、备注或 `searchUrl`
- [ ] A-08 favicon 首选浏览器原生缓存图片，初始化不逐站联网抓图
- [ ] A-09 Pages 的书签 hover favicon 旋转属于必须保留的反馈
- [ ] A-10 最终视觉验收必须使用真实 214 条数据与 Image #1 对照
- [x] A-11 编辑模式采用成熟 Vue 组件库处理 Dialog、Select、Combobox、TagsInput 和焦点管理，不手写基础交互

## B. 浏览态视觉回归

实现后逐项测试：

- [ ] B-01 约 1858x1005 视口下整体构图接近 Image #1
- [x] B-02 页面首屏没有固定侧栏
- [x] B-03 搜索组合水平居中，搜索框约 30rem x 3rem
- [ ] B-04 搜索带使用克制的向下阴影和 Pages 式上下留白，不使用底部细线；向下滚动时顶部区域与内容层级自然，无异常空白感
- [x] B-05 隔离数据下目录呈 4 / 3 / 2 列响应式瀑布分布
- [x] B-06 目录块宽度、细边框、圆角和块间距接近 Pages
- [x] B-07 目录标题为 Pages 的 `1.1rem` 常规字重，浏览态无折叠箭头、目录图标、数量徽章和操作按钮堆叠
- [x] B-08 书签是无独立卡片边框的纵向行
- [x] B-09 favicon 与标题同处第一行，URL 从书签行左缘开始，尺寸与 Pages 一致
- [ ] B-10 无标签和备注的书签不产生额外空白
- [x] B-11 页面没有横向滚动、目录重叠或 Macy 高度错误
- [x] B-12 Macy 使用最短列优先重排，隔离高度不均数据下无明显长尾
- [ ] B-13 主内容悬浮式细滚动条在真实 214 条数据下显示和滚动正常
- [ ] B-14 滚动条覆盖在视口右缘且不参与宽度计算，内容左右 5% 边距视觉一致
- [ ] B-15 标题默认和 hover 状态均不裁字；字号过渡期间文字清晰，垂直位置、URL 和书签行位置保持一致
- [ ] B-16 目录实际高度严格等于 `(内容行数 + 1) × 48px`；展开与折叠使用相同的 10px 内容前距，边框只出现 `0 / 24px` 两种相位

## C. favicon 回归

- [ ] C-01 Chrome / Edge 使用 `/_favicon` 浏览器缓存来源
- [x] C-09 含 `#/` 的 SPA 地址依次生成精确书签地址、去 fragment 地址和站点根地址缓存候选，精确缓存不会被默认图抢先遮住
- [ ] C-10 WXT 使用绝对路径的持久隔离 profile；访问站点并生成缓存后，重启开发浏览器仍显示真实图标
- [ ] C-11 未访问站点且隔离 profile 无缓存时显示本地占位，不把日常 Chrome 缓存误认为当前 profile 可用
- [ ] C-12 访问新站点后点击“刷新全部图标”，所有已渲染、折叠后再展开及搜索结果中的原生图标重新读取当前 profile 缓存，不逐站联网
- [ ] C-13 全量刷新出现真实图标后，刷新插件页面仍显示真实图标，不重新命中刷新前缓存的默认图
- [ ] C-02 214 条数据初始加载没有逐站网络请求风暴
- [ ] C-03 缓存命中时显示真实网站图标，不是统一灰色占位图标
- [ ] C-04 加载中占位尺寸固定，文字不跳动
- [ ] C-05 全部来源失败时显示本地默认图标，不显示破图
- [ ] C-06 hover 书签行时 favicon 持续平滑旋转
- [ ] C-07 手动刷新单项图标时只有该项忙碌，其他图标不闪烁
- [ ] C-08 浏览器缓存图加载前或失败后始终有本地图标占位，缓存命中后原位覆盖且不跳动

## D. 数据缺省回归

使用一份只有目录、标题和 URL 的普通导入数据：

- [ ] D-01 所有书签正常展示和打开
- [ ] D-02 页面不出现空标签侧栏
- [ ] D-03 页面不生成假标签
- [ ] D-04 快捷搜索不根据标题、域名或 Pages JSON 生成假目标
- [ ] D-05 无保留 tag 时显示轻量空状态；单独存在的 `searchUrl` 不产生搜索目标，普通搜索仍可用
- [ ] D-06 编辑并保存 extra 后，仅对应书签出现标签、备注或明确配置的搜索能力

## E. 搜索回归

- [ ] E-01 打开页面后搜索框聚焦
- [x] E-01A 焦点位于页面空白、目录标题或工具按钮时直接输入普通字符，首字符不丢失并进入搜索框；Ctrl / Cmd / Alt 快捷键和模态表单不被劫持，中文输入法组合不产生裸拼音
- [ ] E-02 普通搜索匹配目录、标题、URL、域名、普通 tag 和备注，且不区分大小写
- [ ] E-03 普通文本只过滤主页面，不显示候选浮层；方向键不选择隐藏书签，Enter 始终使用当前搜索引擎
- [x] E-03A 输入 `!` / `！` 后候选只含 `search_site` 站点目标，不含 `search` 普通引擎；当前引擎入口保持可见并执行站点搜索
- [x] E-03B 空输入和普通文本搜索中上下键均循环切换引擎；`#` / `!` 中上下键只切换各自候选
- [ ] E-04 普通过滤后的书签仍可点击打开；Esc 清空并恢复完整浏览内容
- [ ] E-05 当前 `search` 引擎优先，其次浏览器顺序第一项，无配置时回退 Bing
- [ ] E-06 空输入时 `↑` / `↓` 按当前可见引擎顺序循环切换，输入框保持为空
- [ ] E-07 `!` / `！` 展示全部目标但不执行
- [ ] E-08 `!筛选词` 只按标题、域名和普通 tag 筛选目标，不误执行
- [ ] E-09 `! 关键词` 使用当前选择的全部目标列表项执行
- [ ] E-10 `!筛选词 关键词` 正确拆分第一段筛选词与空格后的完整关键词
- [ ] E-11 快捷模式只有目标有效且关键词非空时 Enter 才能打开新标签
- [ ] E-12 `search` 使用有效模板替换 `${keyword}` / `{keyword}`，中文、空格和特殊字符编码正确
- [ ] E-13 `search_site` 使用默认搜索引擎生成 `site:<domain> <关键词>`，不猜站点搜索 URL
- [ ] E-14 `search` / `search_site` 同时存在或配置无效时不执行，并给出局部提示
- [ ] E-15 只有显式 `search_site` tag 才产生快捷目标；`search` 只进入普通引擎列表，不存在旧数据兼容或自动迁移分支
- [ ] E-16 输入 `#` 展示全部真实 tag 候选，并预览所有带 tag 的书签
- [x] E-17 输入 `#xx` 可动态匹配 `xxx`、`xxy` 等候选，前缀优先、包含其次，主内容按候选并集预览
- [x] E-18 查询与已有 tag 完全同名时自动精确筛选；上下键和 Enter 选择候选后只通过完整 `#tag` 文本得到相同结果
- [ ] E-19 修改精确 `#tag` 后若不再完全匹配已有 tag，则回到动态候选；Esc 先关浮层、再次按下清空并恢复全部书签
- [ ] E-20 tag 模式没有候选时 Enter 不执行网络搜索；无 tag 数据时显示紧凑空状态
- [ ] E-21 左侧 tag 入口只在有 tag 时出现；悬浮、点击或键盘聚焦后打开面板，入口横移到面板不闪退，且不挤压内容或触发 Macy 重排
- [ ] E-22 悬浮面板显示 tag 名称和准确数量，可在面板内过滤；点击外部、入口或 Esc 可关闭
- [x] E-23 点击左侧 tag 后只写入 `#tag`，由同一解析器精确筛选、关闭面板并恢复搜索框焦点
- [x] E-24 鼠标点击 tag 与键盘输入并选择同一 tag 的筛选状态一致
- [ ] E-25 `search` / `search_site` 可被 `#` 搜索，在候选和面板中标记为搜索能力
- [ ] E-26 `#` / `!` 候选浮层与搜索框严格对齐、内容状态互斥且不遮挡整个页面；普通文本无浮层
- [x] E-26A 在短名称和长名称搜索引擎之间切换后，搜索下拉的 left、width 仍与搜索框实际边界逐像素一致
- [ ] E-27 候选变化后选择索引不越界，快速输入、上下键、中文输入法和 Esc 无竞态
- [ ] E-28 搜索框无可见 placeholder；引擎入口只显示 favicon 和名称，无下拉箭头、分隔栏或教学文字
- [ ] E-29 自定义 Bing / Google 存在时对应内置项不重复显示；当前选中和菜单高亮使用中性背景，无绿色外框或双重边框
- [ ] E-30 `#` tag、`!` 快捷搜索和引擎菜单使用同一无描边浮层、圆角、阴影及 hover / active / selected 背景；普通文本不渲染浮层
- [ ] E-31 引擎入口默认无外观，悬浮直接打开菜单，横移不闪退；点击引擎立即切换并关闭，菜单不显示“内置”等说明
- [ ] E-32 搜索浮层无教学提示；精确 tag 不使用左侧色条、inset 阴影或第二套选中样式
- [ ] E-33 搜索结果 hover 为浅背景、active 为 selected 背景；鼠标移动到任一项后，上下键从该项继续且 Enter 执行同一项
- [ ] E-34 搜索长列表的滚动槽不占结果宽度，滚轮 / 触控滚动正常，文字不会因滚动条出现而挤压
- [ ] E-35 引擎菜单与 `#` / `!` 候选浮层始终互斥，不重叠、不同时渲染

## F. 折叠与管理回归

- [ ] F-01 单目录展开 / 收起可用
- [x] F-02 浏览态目录头无折叠箭头；右下工具组提供 `Maximize2` 展开和 `Minimize2` 收缩，点击目录标题仍可单独展开 / 收起
- [ ] F-03 搜索命中不被折叠状态隐藏
- [ ] F-04 折叠后 Macy 正确重排
- [x] F-29 tag 与工具入口位于左侧同一纵向轨道，均为 36px 命中区、15px 图形和同值弱底板；工具向下展开，无 tag 时自动上移，展开项不重复叠加阴影
- [ ] F-30 全部展开 / 全部收缩会持久化所有目录状态，页面刷新后保持，Macy 无重叠或错位
- [x] F-05 进入整理态后才显示管理控件，并沿用搜索、tag、折叠状态和滚动锚点
- [x] F-06 整理态普通文本与 `#tag` 均实际过滤画布；查询非空时仍可拖拽，并按可见锚点映射完整数据中的真实位置
- [ ] F-07 目录整理与书签整理互斥，不同时显示两套把手和危险操作
- [x] F-08 目录整理进入时临时默认全部收缩，收缩项只显示统一高度标题行且无 `...` 或空内容；书签整理进入时默认全部展开；两种子模式始终保留单目录及整体展开 / 收缩且不改写浏览态偏好
- [x] F-37 目录整理使用正常流响应式 Grid；拖动副本挂到页面浮层并脱离 Grid，Grid 内只有一个随预测索引移动的等高虚线占位槽，其他目录自动补位，放下顺序与占位一致
- [x] F-38 根目录夹有不可见直属书签时，目录预测槽的可见相对顺序与 Chrome 真实写回顺序一致；切换到书签整理时即使浏览态全部收缩也仍可拖拽
- [x] F-39 书签拖拽使用脱离 ScrollArea 的 fallback 浮动副本；按住拖拽时鼠标滚轮能改变页面滚动位置，放下后真实顺序正确
- [ ] F-40 整理模式-书签收缩后平时仅有标题行；拖书签靠近时仅目标目录动态展开投放区并触发 Macy 重排，离开收回，放下后自动展开显示结果；整理模式-目录仍为纯标题 Grid
- [ ] F-09 书签整理支持目录内排序和跨目录移动，空目录也有稳定落点
- [ ] F-10 拖动项、原位占位、目标目录、插入线和无效区域反馈清楚且不引发布局跳动
- [ ] F-11 长列表边缘自动滚动平滑可控，不越过目标
- [ ] F-12 写回只锁定相关对象，不出现整页 loading；成功反馈局部且短暂
- [ ] F-13 写回失败回滚浏览器数据、视图顺序、DOM 与折叠状态，并保留错误信息
- [ ] F-14 工具条按钮和 `Ctrl/Cmd+Z` 均可撤销最近一次成功移动；输入框 / 弹窗内仍使用系统撤销
- [x] F-15 Esc 每次只处理菜单 / 弹窗、候选、查询、整理态中的最高一层，弹窗关闭不误退出整理态
- [ ] F-16 退出整理态恢复纯浏览布局、原搜索 / tag 条件、滚动锚点和合理折叠状态
- [x] F-17 隔离 Chrome 已验证书签与目录确认删除会真实移除浏览器节点；跨目录编辑追加到目标目录尾部
- [x] F-18 整理态 Macy `trueOrder: true` 保序且保持自然高度，浏览态切回最短列模式，无 CSS Grid 强制等高
- [x] F-19 tag 编辑使用组件库 TagsInput + Combobox，不是逗号字符串文本框
- [ ] F-20 聚焦和输入可补全已有 tag，前缀匹配、包含匹配、次数和名称排序正确
- [ ] F-21 可用鼠标、上下键、Enter 和 Tab 选择建议；Esc 关闭建议且不误关整个编辑弹窗
- [ ] F-22 无完全匹配时可明确创建新 tag；中英文逗号、Enter 和批量粘贴均正确生成 token
- [ ] F-23 tag 去除首尾空白、大小写不敏感去重，输入法组合期间不会提前提交
- [ ] F-24 Backspace 和移除按钮删除正确，token 换行不撑破弹窗，保存失败不丢输入
- [ ] F-25 `search` / `search_site` 在“搜索能力”组中互斥；前者显示并校验模板，后者显示域名预览
- [ ] F-26 新 tag 保存后立即出现在其他书签的自动补全、`#` 候选和左侧 tag 面板中
- [x] F-27 token 与输入位于同一个可换行外框和焦点圈内，不出现上下两个输入区域
- [x] F-28 弹窗目录 Select 与 tag 建议菜单高于 Dialog 和遮罩，关闭层级正确
- [ ] F-31 左侧 tag 面板无外描边、标题栏、分割线和关闭按钮；仅过滤输入保留单层边框，点击 tag 立即筛选并关闭
- [ ] F-32 右下工具悬浮后整个工具组共享一层阴影；点击展开、折叠、刷新和整理立即执行，需要输入的命令直接打开对应模态
- [ ] F-33 目录 Select 与 tag Combobox 保持点击 / 输入 / 键盘打开，菜单视觉与主页浮层一致且不因 hover 误触
- [ ] F-34 编辑、目录、确认和导入导出 Dialog 均为无外描边单层表面；标题、正文、footer 无独立背景或分割线，内容无卡片套卡片
- [ ] F-35 导入导出仅以自然间距排列两个操作行，无分组外框、灰色标题底、行分割线和教学型 intro
- [x] F-36 整理工具条按内容紧凑收缩，只使用一层固定边界且无阴影；分段控件无嵌套边框并以 selected 背景区分，命令连续等距排列，与书签部分留有间距；窄屏不与 tag按钮重叠；无“完成”和复制 URL，含整体展开 / 收缩

## G. Popup 与备份回归

- [ ] G-01 Popup 读取当前标签页标题和 URL
- [ ] G-02 Popup 可筛选目录并保存当前页
- [ ] G-03 保存成功关闭，失败保留并提示
- [ ] G-04 无 bookmarklet 配置时不显示空入口
- [ ] G-05 完整 JSON 导出包含书签结构和已有 extra
- [ ] G-06 导入无 extra 的书签时按空 extra 处理
- [ ] G-07 同名目录合并和完全相同数据去重正确
- [ ] G-08 无效文件失败时现有数据零修改

## H. 响应式与工程回归

- [x] H-01 390px 下单列目录，无横向溢出
- [x] H-02 搜索和工具入口不重叠
- [ ] H-03 Popup 固定宽度内无溢出
- [ ] H-04 浏览器原生焦点 outline 全面隐藏，但键盘焦点语义、组件自有状态、不可见无障碍名称、弹窗 Esc 和危险确认仍可用
- [x] H-09 Esc 按单层优先级恢复：弹窗、交互浮层、候选、查询、整理态依次退出；正在导入时不破坏写入
- [x] H-05 类型检查通过
- [x] H-06 生产构建通过
- [ ] H-07 WXT 真实扩展测试通过
- [ ] H-08 真实 214 条数据截图由用户确认

## 当前阻断

- 本轮统一表面语义与最短操作路径正在实现，E-30 至 E-32、F-29、F-31 至 F-36 尚待 WXT 真实页面验收。
- 最新 `vue-tsc`、8 项搜索 / favicon 逻辑测试和 WXT Chrome MV3 生产构建通过；引擎 Popover 的悬浮、横移与点击仍以用户当前 WXT 页面实测为准。
- 隔离 Chrome MV3 已实测：引擎 / 搜索同时可见浮层总数为 1；搜索展开层 `offsetWidth === clientWidth === 480px`；hover 与 active 实际背景分别为 `rgb(238, 241, 239)` / `rgb(216, 227, 222)`；鼠标从第 2 项移入后方向键落到第 3 项。
- 隔离 Chrome MV3 已实测：左右入口默认背景均为 `rgb(245, 246, 245)` 且无阴影；Esc 可分别关闭搜索、引擎、tag、工具组、整理态、书签 / 目录 Dialog、删除确认和导入导出，并恢复搜索框焦点。
- 最新 WXT dev 已使用项目内持久 profile 完整重启并成功构建；最终页面视觉与用户真实数据体验待用户确认。
- 普通文本无浮层的隔离 Chrome 回归通过：`normalOverlayCount = 0`，页面保留 18 条匹配书签；切换为 `#` 后候选浮层出现，滚动宽度、active / hover、引擎互斥和 Esc 回归继续通过。
- 2026-07-30 整理态隔离 Chrome 回归通过：Macy 保序绝对定位生效；普通搜索和 `#work` 各精确过滤到 1 条；弹窗 Esc 后整理态仍存在；书签 / 目录节点真实删除；跨目录编辑后的节点是目标目录最后一项；整体展开 / 收缩有效；页面无控制台错误。
- 2026-07-31 筛选拖拽隔离 Chrome 回归通过：同目录匹配项跨越隐藏项排序、跨目录移动到可见锚点之后、筛选目录跨越隐藏目录排序均写回预期完整顺序；隐藏项相对顺序不变，页面无控制台错误。
- 2026-07-31 搜索与左侧轨道隔离 Chrome 回归通过：目录标题 / 功能按钮聚焦时首字符进入搜索框，模态内输入不污染主页；普通搜索 ArrowDown 从 Bing 切到 Google；`!` 只显示 Docs `search_site` 且当前引擎可见；长短引擎切换前后搜索框与搜索下拉 left / width 完全相等；tag按钮与功能按钮横坐标均为 10px，纵坐标为 154px / 196px，命令从 235px 向下展开。
- 2026-07-31 整理工具条隔离 Chrome 回归通过：桌面宽度 314px、分段边框 0px、底部间距 18px；390px 窄屏工具条左边界 54px、tag按钮右边界 42px，保留 12px 间距且无内部溢出；整理态 CRUD、Esc、整体展开 / 收缩及 Macy 保序回归继续通过，无控制台错误。
- 2026-07-31 整理拖拽专项回归通过：浏览偏好预设 8 个目录全部收缩，书签整理仍默认展开且收缩 / 再展开后重建 Sortable；按住书签拖拽时滚轮使 ScrollArea `scrollTop` 从 0 变为 520。目录整理 7 个可见目录均为 38px 标题行，浮动副本位于 `BODY`，唯一虚线槽预测索引 6；根目录夹有直属书签时 Chrome 最终目录相对顺序仍与预测一致。
- 2026-07-31 子模式布局隔离回归通过：整理模式-书签整体收缩后 8 个目录高度均为 38px，Macy 纵向坐标仅为 0 / 62 / 124，无 `...` 空内容和大空档；随后切到整理模式-目录仍为 Grid，目录预测和真实写回回归继续通过。
- 用户真实 214 条数据与 Image #1 的最终视觉对照尚未完成。
- 浏览器真实 favicon 缓存命中、整理拖拽写回/撤销和完整导入导出仍需在 WXT dev 浏览器人工验证。
- 用户确认前不提交本轮业务代码。
