# 书签插件当前阶段测试清单

本文档只用于当前阶段手工测试。只有已经实现并已进入当前构建产物的内容，才会进入“当前可测”区域。尚未实现或尚未稳定接线的内容，统一放到“待实现后补测”区域。

## 现在只看什么

为避免本轮范围继续发散，当前这一轮先不安排新的用户点测，先做样式统一：

- 当前里程碑：`M-08`
- 当前实现中的工作项：整体交互样式统一

当前不用再重复验证：

- `M-06-01` 已通过
- `M-06-02` 已通过
- `M-06-03` 已通过
- `M-06-04` 功能本身可用，原反馈改为“移动方式不够明显”
- `M-06-05` 已通过
- `M-06-06` 已通过
- `UI-04` 已通过
- `M-08-01` 已通过
- `M-08-02` 已通过
- `M-08-03` 已通过
- `M-08-04` 已通过
- `M-08-05` 已通过

所以这轮先不新增手工测试项，等样式统一落地后再切出新的当前可测范围。

使用方式：

- 测试通过后，将对应项改为 `[x]`。
- 测试失败时，保留 `[ ]`，并在“结果记录”里写失败现象。
- 一项一个结果，不要合并判断。

## 当前阶段范围

当前需要测试：

- `M-01` 扩展骨架可加载
- `M-02` 真实书签只读打通
- `M-03` Popup 保存当前页可用
- `M-04` 书签搜索与站内搜索可用
- `M-05` 书签编辑写回可用
- `M-06` 整理模式最小闭环（仅书签整理部分）
- `M-07` 导入导出最小闭环

当前不要测试：

- `M-06` 目录整理、目录排序、目录重命名、目录删除
- `M-08` 综合验收

## 当前可测

### UI 基准补充复测

- [x] UI-01 书签卡片优先显示浏览器标准 favicon；Chrome 走原生图标，Firefox 保持兼容兜底；图标容器不带背景，且周围留白已收紧
结果记录：用户已验证通过。

- [x] UI-02 主页面与 popup 的全部文字都已统一切到 `Sarasa Mono SC` 优先栈
结果记录：用户已验证通过。

- [x] UI-03 书签地址单独占一行，并从卡片内容最左侧开始对齐，不与标题保持同一缩进
结果记录：用户已验证通过。

### M-01 扩展骨架可加载

- [x] M-01-01 `npm run build` 产物存在且无构建失败
结果记录：

- [x] M-01-02 浏览器成功加载 `.output/chrome-mv3/`
结果记录：

- [x] M-01-03 浏览器工具栏中能看到扩展按钮
结果记录：

- [x] M-01-04 点击扩展按钮后 popup 正常打开
结果记录：

- [x] M-01-05 popup 中打开主页面入口可用
结果记录：

- [x] M-01-06 主页面打开后无白屏
结果记录：

- [x] M-01-07 扩展页、popup、扩展错误页中无明显脚本报错或权限报错
结果记录：

M-01 阶段结论：

- [x] M-01 通过，可以进入 M-02

### M-02 真实书签只读打通

测试前准备：

- 浏览器默认书签位置下至少准备两个一层目录。
- 每个目录下至少准备两个真实书签。

- [x] M-02-01 主页面能读取并展示默认书签位置下的一层目录
结果记录：

- [x] M-02-02 每个目录下的书签卡片能正常显示标题
结果记录：

- [x] M-02-03 书签卡片能正常显示域名或 URL 信息
结果记录：已复测通过；当前阶段问题已修复。

- [x] M-02-04 目录展开 / 收起可用
结果记录：已复测通过；当前阶段问题已修复。

- [x] M-02-05 搜索框默认聚焦
结果记录：

- [x] M-02-06 搜索浮层与搜索框左边界、宽度保持一致
结果记录：

- [x] M-02-07 无数据或无结果时，页面不会出现明显错位或白屏
结果记录：

M-02 阶段结论：

- [x] M-02 通过，可以继续推进 M-03 / M-04

### M-03 Popup 保存当前页可用

测试前准备：

- 打开一个普通网页标签页。
- 确保浏览器书签中存在可保存目标目录。

- [x] M-03-01 popup 能读取当前标签页标题
结果记录：

- [x] M-03-02 popup 能读取当前标签页 URL
结果记录：

- [x] M-03-03 popup 能展示目录列表
结果记录：

- [x] M-03-04 popup 目录搜索可用
结果记录：

- [x] M-03-05 点击目录后，当前页能保存到对应目录
结果记录：

- [x] M-03-06 保存后 popup 能关闭或给出轻量反馈
结果记录：已复测通过；当前阶段问题已修复。

- [x] M-03-07 浏览器原生书签管理器中能看到新增书签
结果记录：

M-03 阶段结论：

- [x] M-03 通过，可以继续推进 M-04

### M-04 书签搜索与站内搜索可用

测试前准备：

- 至少准备一个带 `searchUrl` 的书签。
- 如需验证标签、备注搜索，准备带标签或备注的书签额外数据。

- [x] M-04-01 普通关键词能搜索书签标题、URL、标签、备注
结果记录：

- [x] M-04-02 搜索结果浮层支持键盘上下选择
结果记录：

- [x] M-04-03 普通搜索模式下，默认第一行是搜索引擎结果；未手动切换高亮时，按 `Enter` 直接触发搜索引擎搜索
结果记录：用户已验证通过。

- [x] M-04-04 按 `Esc` 能清空搜索并关闭浮层
结果记录：

- [x] M-04-05 输入 `!筛选词` 时，只使用筛选词过滤带 `searchUrl` 的目标站点；此时没有搜索内容，按 `Enter` 无效
结果记录：用户已验证通过。

- [x] M-04-06 输入 `!筛选词 搜索内容` 时，第一个空格后的内容全部视为搜索内容；快捷站内搜索支持键盘上下选择与 `Enter` 打开
结果记录：用户已验证通过。

- [x] M-04-07 可通过搜索引擎结果项触发 Google / Bing 网络搜索
结果记录：

M-04 阶段结论：

- [x] M-04 通过，可以继续推进 M-05

### M-05 书签编辑写回可用

测试前准备：

- 至少准备两个一层目录。
- 至少准备一个可编辑的真实书签。
- 新增入口：顶部“书签”按钮。
- 编辑 / 删除入口：当前通过“整理”模式下的书签卡片操作按钮验证。

- [x] M-05-01 可新增书签，并写回浏览器书签目录
结果记录：

- [x] M-05-02 可编辑书签标题、URL、所在目录，并写回浏览器书签
结果记录：

- [x] M-05-03 可保存标签、备注、`searchUrl`，刷新后仍能读回
结果记录：

- [x] M-05-04 删除书签后，浏览器书签本体删除，插件额外数据同步清理
结果记录：

- [x] M-05-05 刷新页面后，新增/编辑结果不丢失
结果记录：

M-05 阶段结论：

- [x] M-05 通过，可以继续推进 M-06

### M-06 整理模式最小闭环

测试前准备：

- 至少准备两个一层目录。
- 每个目录下至少准备两个可拖拽书签。

- [x] M-06-01 可进入和退出整理模式
结果记录：

- [x] M-06-02 整理模式下书签卡片显示拖拽把手、编辑、复制、删除等整理态控件
结果记录：

- [x] M-06-03 支持目录内拖拽排序，并写回浏览器书签顺序
结果记录：用户已复测通过；同目录向后拖拽排序问题已修复。

- [x] M-06-04 支持跨目录拖拽移动，并写回浏览器书签目录归属
结果记录：用户补充说明，跨目录拖拽功能本身没有问题；原反馈是“目录间移动方式不够明显”。

- [x] M-06-05 整理模式样式已收口，当前已知问题包括下方空白、拖拽按钮观感、整理态整体对齐
结果记录：

- [x] M-06-06 最近一次移动或排序撤销
结果记录：用户已复测通过；按钮撤销与 `Ctrl/Cmd + Z` 都可用。

- [x] UI-04 修改、移动、删除等写回操作后，页面不出现明显整页加载态、整块闪动或“像重载了一次”的体感
结果记录：用户已复测通过；删除后其余卡片不再出现图标闪动。

别的问题：
- 目录间移动方式不够明显，需要补充更清晰的拖拽引导或提示
- 整理模式新增书签、新增目录和上面的按钮功能冲突，不需要
- 复制按钮作用未知，如果是复制地址则不需要
- 编辑框无法ESC退出
- 标签分割需要支持中文逗号
- 浏览页面的书签右上角的按钮“更多”还是不知道有什么用，点击无反应
- 提示信息不要用浏览器自带
- 标签需要支持基于历史标签提示
- 内置两个标签，search 和 site，search用于作为搜索引擎出现在搜索栏，site用于支持被site:搜索；如果没有配置搜索引擎则默认bing
- 

M-06 阶段结论：

- [x] M-06 通过，可以继续推进 M-07

### M-07 导入导出最小闭环

测试前准备：

- 在主页面左下角打开“设置与导入导出”。
- 准备一份可导入的全量 JSON 测试文件。
- 如需验证全量导入回放，先准备带标签、备注、`searchUrl` 的测试书签。

- [x] M-07-01 全量数据导出
结果记录：用户已验证通过；文件名包含日期时间。

- [x] M-07-02 全量数据导入
结果记录：用户已验证通过；全量导入可用。

- [x] M-07-03 同名目录合并
结果记录：用户已验证通过；同名目录合并正常。

- [x] M-07-04 新增内容追加到目录末尾
结果记录：用户已验证通过；新内容追加到目录末尾。

- [x] M-07-05 完全相同数据去重
结果记录：用户已验证通过；完全相同数据会跳过不重复导入。

M-07 阶段结论：

- [x] M-07 通过，可以继续推进 M-08

### M-08 当前轮：目录新增 / 重命名 / 删除

测试前准备：

- 进入主页面。
- 至少准备一个可重命名或删除的目录。
- 如需验证删除，准备一个目录内含书签与标签/备注等额外数据的测试目录。

- [x] M-08-01 可新增目录，并写回浏览器书签
结果记录：

- [x] M-08-02 可重命名目录，并写回浏览器书签
结果记录：

- [x] M-08-03 删除目录前会出现自定义确认弹窗
结果记录：用户已确认当前删除确认弹窗恢复正常；后续观感问题转入整体交互样式统一轮次处理。

- [x] M-08-04 确认删除后，目录及其内部书签会从浏览器书签中删除
结果记录：

- [x] M-08-05 删除目录后，相关额外数据会同步清理，页面本地状态也会更新
结果记录：


## 待实现后补测

以下内容属于需求范围，但当前版本还不应该要求你测试通过：

- [ ] M-06-07 目录整理、目录排序、目录重命名、目录删除

进入“当前可测”的条件：

- 功能已经实现。
- 功能已经接到当前扩展构建产物。
- 文档已明确说明测试入口和通过标准。

## 当前阻塞与备注

记录当前阶段测试时发现的问题：

- 目录新增 / 重命名 / 删除当前轮已通过。
- 当前新的问题域不是单点功能失败，而是全局交互样式不统一；待样式统一实现后再切新的可测清单。

- `M-06` 当前阶段已通过。
- `M-07` 当前阶段已通过。
- 当前只剩 `M-08-03`：删除目录确认弹窗样式需要优化，当前重点看危险态顶部强调、正文警示区和整体节奏是否统一。

## 已实现待复测的 UI 基准项

以下内容不回退 `M-01`、`M-02`、`M-03` 的通过状态，但已经进入当前构建产物，需要先完成复测，再继续做更大范围的 UI 判断：

- [x] 标准 favicon 来源、无背景图标与收紧后的图标留白
- [x] 全量文字统一使用 `Sarasa Mono SC` 字体优先栈
- [x] 地址最左侧独立对齐
