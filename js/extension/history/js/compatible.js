import { add, bulkAdd, updateTitle, queryList, clearTable, exportTsv } from './compatible.dexie.js';

export default {
    add,
    bulkAdd,
    updateTitle,
    queryList,
    clearTable,
    exportTsv
};
